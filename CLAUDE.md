# 모음 프로젝트 개발 규칙

## 라이브러리 / 프레임워크 버전

최신에 가까우면서 안정성이 보장된 버전을 선택한다.
GA / Stable 릴리즈 기준. RC, Beta, Snapshot은 사용하지 않는다.

버전을 올리거나 새로 채택할 때는 아래 순서로 판단한다 (2026-08-02, Spring Boot 4 마이그레이션에서 정립).

1. **GA/Stable 기준은 배포 채널뿐 아니라 개별 API 단위에도 적용한다.** 컴파일러/런타임 자체는 GA여도 특정 함수·클래스가 `@Experimental`류로 마킹돼 있으면 그건 그 라이브러리 안의 RC/Beta다. "GA 버전에 포함됐는가"가 아니라 "그 API 자체에 Stable 딱지가 붙었는가"로 판단한다.
2. **타겟 버전 자체에 벤더가 인정한 결함(회귀 이슈, 이슈 트래커에 기재된 버그)이 있는지 확인한다.** 있으면 그 결함이 없는 바로 아래 버전까지만 올린다. 반대로 특정 패치가 "직전 버전의 결함을 고친 릴리즈"라면 그게 가장 안전한 선택이 될 수 있다 — "최신"과 "안전"이 항상 같은 방향은 아니므로 매번 따로 확인한다.
3. **의존성 하나만 올려도 다른 의존성의 최소 버전 요구(floor)에 걸릴 수 있다.** 업그레이드 순서를 정하기 전에 실제 빌드 설정을 확인하거나 빌드를 돌려서 이 결합관계부터 파악한다(예: Spring Boot 4.1은 Gradle 8.14+/Kotlin 2.2+를 요구 — "이것만 먼저" 계획이 성립하지 않을 수 있음).
4. **Breaking change가 벤더의 공식 확정 방향이거나 보안 강화 목적이면 회피하지 않고 코드를 고쳐서 따라간다.** "코드 수정으로 해결되는 문제"와 "벤더가 인정한 결함이라 버전을 못 올리는 문제"를 구분해서, 전자는 비용이 들어도 최신을 따라간다.
5. **메이저 버전 업그레이드는 유닛 테스트 통과만으로 끝내지 않고 실제 기동 스모크 테스트(로컬 DB 포함)까지 검증한다.** 빌드 성공과 테스트 통과는 필요조건이지 충분조건이 아니다 — 자동설정이 조용히 스킵되는 유형의 문제는 실제로 앱을 띄워 요청을 보내봐야만 드러난다.

## 참조 정보 관리

개발 중 확인된 버전, 설정값, 도구 위치 등 나중에 다시 찾아야 할 정보가 생기면
memory에 reference 타입으로 즉시 추가한다.
